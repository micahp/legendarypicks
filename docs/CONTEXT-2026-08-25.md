# 2026-08-25: Legendary Picks, the night pass

Written 02:11 CDT. Continues `docs/CONTEXT-2026-08-24.md`, which covers the morning,
midday and evening of 08-24 and is still the index for anything not touched here.

## THE NIGHT'S ONE SHAPE

**Every significant error tonight, mine and codex's, was a claim whose SURFACE was
smaller than the claim built on it.** Not wrong data. Correct measurements, quoted
about more than they covered.

| the claim | the surface it actually had |
|---|---|
| "Leagues Cup has games and props, `/props` can't reach them" (me, first message) | never measured. **Zero lcup props have ever existed in either database** |
| "RotoWire publishes soccer as one bucket, zero MLS" (me, in the design doc AND the roadmap) | ONE light archive day (08-24). Across seven days it quotes MLS on four and Liga MX on three |
| "The diagnoses hold, land it" (me, audit verdict) | backend only. Never ran `verify-gates.sh`, never rendered a page |
| "100 backend tests pass" (codex) | 38 modules never loaded at all, three of them its own new tests |
| "Ran 0 tests ... OK" (me, prod DB suite) | my command lost its working directory. Zero tests, reported green |
| "NFL settlement: 76 of 80 settled" (me, reporting a gain) | 76 on both sides. Real code fix, **no dataset delta** |

The last two are the tell. I wrote up codex's 38 unloaded modules as a finding, then
produced the identical failure myself twenty minutes later. **A count is the evidence; a
verdict is not.**

---

## 1. Production was three releases behind and nothing said so

Prod ran **v0.8.5 code** from 08-21 until tonight. v0.8.6 and v0.8.7 were tagged, their
changelogs written, and their code never deployed. Proved by content rather than dates:

```
md5 /app/ingest_scoreboards.py in the container   c3aa055ab7fe0b89148c7e5b9e70c1bd  == v0.8.5
md5 backend/ingest_scoreboards.py @ v0.8.7        d5b840308b65efe19c50102a1d6a4d23
```

**This did not contradict the changelog, which is what made it invisible.** The v0.8.7 work
was largely systemd units and ingest runs, which are host and data level and did take
effect. "v0.8.7 shipped" was true of the scheduling half and false of the code half, and
nothing on either database distinguishes them.

Twelve backend modules changed across those two releases and none were running, including
`_core.py`, `scoreboard_store.py`, `core_stories.py` and `routers/games/game_detail.py`.

**v0.8.8 and v0.8.9 both cut and deployed tonight.** Prod now runs current code. This was
the first time in three releases it received any.

## 2. Two scoreboard defects, both diagnosed rather than guessed (v0.8.9)

- **Back from a game detail lost the day and the league filter.** `pages/scores.tsx` READ
  `?date=` and `?league=` into state and never wrote either back; there is no `router.push`
  anywhere in the file. So back returned to a bare `/scores`, which resets to today. Fixed
  with a shallow query write on every state change, defaults kept out of the URL, `?live=1`
  preserved.
- **A game detail tab that failed once stayed empty forever.** Two defects, both rendering
  an EMPTY tab rather than raising, which is why it read as missing data. The load latch was
  keyed on the tab name alone and Next.js reuses the page component across
  `/game/[league]/[gameId]` navigations, so moving between games kept the previous game's
  latch. It was also set BEFORE the await resolved with nothing catching a rejection.
- **The backend was never at fault**, measured against prod for game `401816603`: boxscore
  2 teams and 4 player groups, playbyplay 9 periods, gameinfo venue and attendance.

## 3. The MLB props hole: a dead timer, not a linker

Two past MLB games served an empty Props tab. Not a linking defect:

```
08-21  14 games  1,166 props  1,113 graded    <- last good day
08-22   0 games      0 props                  <- 401816628 TOR @ NYY
08-23   0 games      0 props                  <- 401816653 MIN @ SD
08-24  10 games    988 props    281 graded    <- 3 of 10 final, exactly those 3 graded
```

Every MLB `prop_games` row that has ever lacked an `espn_event_id` is three rows total, none
in this window; 08-20 is 7 of 7 linked. **There was nothing on 08-22 or 08-23 to link.**

Cause: MLB props are 100% Bovada (5,592 of 5,592 rows since 08-18) and the Bovada timers sat
dead from 2026-08-21 11:08 until un-parked on 08-24 (`CONTEXT-2026-08-24` §2). 08-24 grading
is correct, not lagging.

**Recovery is blocked on a build, not a run.** `ingest_rotowire_props.py` has a
`--from-archive` flag that reads local files and spends zero requests, but `LEAGUES` contains
only `nfl` and `mls`. I told Micah this was "a run, not a build" before checking. It is a
build. Task written and handed to codex: `docs/TASK-mlb-rotowire-props.md`.

## 4. Tennis rankings: the seed is already published and we discard it

- **`competitors[].curatedRank.current`** rides on the tennis scoreboard payload we already
  fetch. Measured: Winston-Salem men's singles 16 of 118 competitors, values exactly 1..16,
  so it is the tournament SEED, not a world ranking. `espn_client/scoreboard.py:270` builds
  `{abbrev, name, score}` and drops it, along with the ESPN athlete id.
- **World rankings exist and verify against both publishers.**
  `sports.core.api.espn.com/v2/sports/tennis/leagues/{atp,wta}/rankings`. The athlete id is
  in the `$ref` URL, so it joins to the scoreboard competitor with **no name matching and no
  per-athlete fan-out**. Verified rank for rank: **WTA 100 of 100 points-exact against
  `api.wtatennis.com`; ATP 50 of 50 against tennisexplorer.com.**
- **Three traps measured**: 150 is a hard cap (`&limit=1000` still returns 150); ESPN
  publishes ONE week, not a series (33 and 35 both 404), so `captured_at` must be in the
  primary key or we get no history; and do not key the table on player name the way
  `ufc_rankings` does.
- **Dead ends, so nobody spends the requests again**: every `atptour.com` host including
  `app.atptour.com/api/v2/gateway` is Cloudflare-403 from this box, `api.atptour.com` does
  not resolve, `api.sofascore.com` is walled, `live-tennis.eu` is 403, and **Jeff Sackmann's
  `tennis_atp`/`tennis_wta` repos no longer exist** (that account now holds one repo).

## 5. Sport-first navigation, and the premise I got wrong

The design conclusion survives: RotoWire genuinely uses one `Soccer` label with no
competition field, so the top level should be the sport. **The evidence I built it on did
not.** See the table at the top.

Codex implemented it, corrected the registry (`espn_client/config.py`, not
`espn_leagues.py`, which holds only MLS and NCAAF), and then fixed the nav defect Micah
found by rendering it. Its own comment is the better statement of the rule:

> Props navigation describes the product we offer, not whichever competitions happen to have
> rows in today's database. Row presence is feed-dependent. Using props as the enablement
> registry made both disappear while leaving Soccer visible solely because MLS retained
> historical rows.

## 6. Leagues Cup props: RotoWire has them and we discard them

> **SUPERSEDED 2026-08-25 (later pass). The headline is false.** This section counted
> *clubs present in a day's relay* and inferred a *fixture* from co-presence. Resolving
> both sides of all 130 archived fixtures against the published lcup memberships returns
> **zero cross-border pairings on every one of the seven days**. The Liga MX clubs cited
> below played each other, not MLS: 08-21 was Tigres UANL vs Atlante, Santos Laguna vs
> Puebla and América vs Juárez; 08-23 was Pumas UNAM vs Club Necaxa. That is the
> **Apertura**, Liga MX's own season, which runs concurrently with Leagues Cup. Liga MX
> clubs appear in the relay every week because Liga MX plays every week.
>
> The live payload confirms it for that endpoint: `Toluca` 0 occurrences, `Columbus` 0,
> `América` 0, `Cup` 0. We cannot be discarding strings that never arrive, so the
> mechanism described below is not what is happening.
>
> **But do not read that as "the props do not exist." They do.** `lines.php` takes no
> parameters and is a heavily truncated view: **PrizePicks is a book inside this relay**
> (2,355 lines today, second only to Sleeper), while the relay's *entire* Soccer
> coverage is **21 athletes** across four European fixtures, quoted by exactly two books
> (`prizepicks` 30 lines, `underdog` 8). PrizePicks can carry a full Leagues Cup slate
> while this endpoint shows 21 La Liga and Ligue 1 players, and both are true at once.
> Micah confirmed a Leagues Cup board on PrizePicks directly on 2026-08-25.
>
> So this section's *instinct* was right and its *mechanism* was wrong. We do not hold
> Leagues Cup props, and the reason is neither `kind: club` nor a 236-row refusal: it is
> that **we only ever see PrizePicks secondhand, through a relay that truncates it.**
> Underdog and Bovada have their own ingests (`ingest_underdog_props.py`,
> `bovada_scraper/`). PrizePicks has none. That is the actual gap, and it is a statement
> about which endpoint we ask, not about the market.
>
> The 236-row refusal is real, but its cause is not this: those rows are EPL, Serie A,
> La Liga, Ligue 1 and Liga MX domestic. The ingest was refusing correctly for a
> different reason than the one recorded here, and that wrong reason was handed to codex
> as a task -- pointed at widening a vocabulary, when the fetch is what needs widening. Note that §1 of this same document already recorded "zero lcup props have
> ever existed in either database" and flagged it **never measured**; this section then
> asserted the opposite, also without measuring, using club counts as a proxy for
> fixtures.
>
> What is now true: as of `25a6674`/`0746e83` an `lcup` league with two spines exists,
> and it accepts both real remaining fixtures (Toluca vs Austin FC, América vs Columbus
> Crew, ESPN 401911271/401911270, both 08-26 ET) with **0 unresolved players**. There is
> nothing yet to accept.

Scanning all seven archived relay days rather than one:

```
08-19  42 clubs  MLS 26                 LigaMX -
08-21  54 clubs  MLS 10                 LigaMX Tigres UANL, Santos Laguna
08-22  71 clubs  MLS 16                 LigaMX Santos Laguna
08-23  39 clubs  MLS New England        LigaMX Club Necaxa, Pumas UNAM
```

Liga MX quoted alongside MLS **is** Leagues Cup. We hold zero because `LEAGUES["mls"]` is
`kind: club` and resolves against MLS only, so a fixture with one Liga MX side never resolves
and is dropped as `unknown_team`. **That is the 236-row refusal the v0.8.7 changelog
records.** The ingest is refusing correctly; it has no `lcup` league to file them under.

The discriminator already exists for Bovada at `bovada_scraper/parsers.py:218`, with the
reason: filing Leagues Cup under `mls` mints players no MLS spine can resolve. Task appended
as an addendum to `docs/TASK-mlb-rotowire-props.md` and handed to codex.

## 7. The audit I did badly, and the one codex did well

I audited the branch backend-only: verified three publisher claims against cached payloads,
ran the suite, checked for out-of-repo changes. **I never ran `verify-gates.sh` and never
rendered a page**, then said "land it". Micah rendered it and found the nav defect in
minutes.

Merged, then reverted on his instruction. `dev` back at `f34f85e`; all 24 commits safe on
`feat/sport-first-navigation`; the TTL fix saved as a patch.

**The gates were already failing before the merge**, byte-identical on both sides:

```
branch (ff38374)   -- 25 passed, 7 failed --
dev, no branch     -- 25 passed, 7 failed --
```

So the merge caused none of them and the revert fixed none of them. That does not excuse
not looking.

**Codex's own settlement audit is the model.** It A/B'd against current dev, ran only branch
code against a disposable clone of production, recomputed every new result independently
without importing settlement code, and **rejected its own comparison as invalid** when live
ingestion mutated the source mid-test. Its findings, all verified here:

1. **Settlement is unscheduled.** 20 timers armed, none settle; the only unit matching
   "settle" on the box is `systemd-udev-settle.service`. Users get none of the gains.
2. **The World Cup voids are not evidence-backed.** `repair_world_cup_results.py:42` is
   literally `"retained_voids": reasons.get("no_player_log", 0)`. Missing ingestion and a
   real DNP are indistinguishable to it.
3. **Tennis pending counts lie.** `settle_game.py:93` returns `"pending": 0` when no snapshot
   exists, so those props are counted as neither settled nor pending and vanish from the
   summary. 34 ATP and 39 WTA were unaccounted for while the run printed clean.
4. UFC: no delta (139 both sides). NFL: code fix, no dataset delta (76 both sides).

Real gains are narrower and genuine: **+98 ATP, +52 WTA, +267 World Cup, 417 numeric
outcomes**, idempotent, no source-table mutation.

## 8. Timers: 20 to 14, and the duplication was never per league

Only 4 of 20 timers were league-scoped. The rest already handle every league in one run;
`ingest_scoreboards.py` covers ten leagues per invocation. **The duplication axis was
dev/prod**, six times over.

```
nfl-adp{,-prod} + nfl-transactions{,-prod}   4 -> 1  legendarypicks-nfl-daily   04:10
news{,-prod}                                 2 -> 1                             03:35
news-x{,-prod}                               2 -> 1                             0/2:15
scoreboards{,-prod}                          2 -> 1                             *:00/10
scoreboards-live{,-prod}                     2 -> 2  LEFT SPLIT, see below
```

**Sequential beats staggered.** The dev/prod offsets existed to keep both off ESPN in the
same minute, and §11 of 08-24 proved an offset only MOVES a collision. One job running the
environments in order guarantees they never overlap.

**Measured before touching the scoreboard pair**, because consolidating could have fought
the staggering added that same day:

```
ingest_scoreboards: 755 runs, requests per run median 2, max 118
all-proc per minute: median 8, p90 38, max 122
403 onset (measured 08-19): ~63 in the prior 60s
minutes at or above 60: 20 of 380
```

At a median of 2 requests per run, both environments together is ~4 against a threshold of
63. Safe.

**`scoreboards-live` deliberately left as a pair.** It fires every 60 seconds with
`TimeoutStartSec=120`. Consolidating doubles per-invocation work on a job with no headroom,
trading a race between two units for a queue inside one, on the sport with live scores four
days before NCAAF opens.

**Error handling reuses `news-lib.sh` rather than copying it**, which is what that file's own
comment asks for. `run_step` gained an optional `LP_STEP_LABEL` prefix so a job running one
script against two databases can tell its failures apart; unset, every existing caller is
unchanged, proved both ways. The two levels it already implements are exactly what was
wanted: a step logs `OK`/`TIMEOUT`/`WARN` and always returns 0 so later steps still run, and
`finish()` fails the JOB naming what broke.

Every unit was tested against the command **systemd actually parsed** (`systemctl show -p
ExecStart`), not against my reading of the file, with both a success path and a DEV-fails
path proving PROD still runs and the unit still goes red. All 41 unit files backed up to
`ops/systemd-backup-20260825-010806` first.

---

## 9. Open

**Blocking the branch** (codex's findings, §7): settlement needs approved recurring
execution; the pending count must stop hiding snapshot-less props; WC voids need durable
roster evidence.

**Named tonight, not started:**
- **`REG-pool` is at 1,027 against a ~4,475 baseline.** QB 127 vs 470, RB 239 vs 1123, WR 371
  vs 1791. A 77% shortfall in the NFL draft pool, during draft season, failing identically on
  plain dev. Unrelated to anything else tonight and **the most product-damaging item open.**
- `run_pipeline.py` should become the scheduled entrypoint. It already owns ingest → link →
  settle → stats → coverage and **nothing references it**. Its `step_ingest_props` is stale
  (calls `bovada_scraper mlb` directly, bypassing the provider registry).
- `mlb-capture` writes to `picks.db` with no `-prod` in its name. Unit names do not reliably
  indicate environment.
- Four jobs run in ONE environment only, not obviously deliberately: `game-recaps` and
  `rotowire-probe` dev-only, `history-prod` and `ufc-fight-stats-prod` prod-only.
- The 4 consolidated units have not yet run in production. First fires: scoreboards *:00/10,
  news-x 0/2:15, news 03:35, nfl-daily 04:10. Only the DEV half of nfl-daily was proven live.
- `backend/picks.db` and `backend/picks.dev.db`, the two 0-byte strays, still untracked and
  **still not gitignored**.
- `docs/AUDIT-sport-first-navigation-2026-08-25.md` is pushed and is wrong where §7 corrects
  it. Not yet amended.

---

# 2026-08-25/26, the evening and overnight pass

## 10. THE EVENING'S ONE SHAPE

**A missing thing recorded as a measured thing.** Six times, and I made every one:

| I concluded | It actually was |
|---|---|
| the relay lists `prizepicks`, so we can reach PrizePicks | we reach what RotoWire *republishes* — 22 props on one La Liga game |
| ESPN publishes no tackles for this competition | the *summary* doesn't; the *core api* publishes 108 fields |
| three phases published 0 events | they returned **403** on the retry — a refusal, not an empty collection |
| a bench match is 0 shots | it is an absence, and it was dragging every hit rate down |
| 73 of 103 Liga MX players have chart coverage | I counted **log rows**; the endpoint refused those rows regardless |
| the FotMob run inserted 795 rows | it wrote **131** — `INSERT OR IGNORE` dropped the rest silently |

The rule was already written, in this repo, in the file it kept happening to.
`ingest_soccer_logs.py` says *"a gap is a statement about which endpoint we asked"* — and
that is the ingest whose 14-field surface I then generalised to the publisher.

**The reusable form:** "we now read everything this endpoint has" is not "we now read
everything", and **a count of fields is a property of a document, never of a source.**

## 11. Leagues Cup props: shipped, v0.9.0

The morning position was *no reachable source publishes Leagues Cup player stat props*.
That was true and it was the wrong frame. PrizePicks had all four fixtures priced the
whole time; the obstacle was an IP range.

- **PrizePicks blocks this datacenter estate-wide.** `api`, `partner-api`, `app`, `www` all
  403 with a **byte-identical** error id, and a path that does not exist returns 403 rather
  than 404 — the block fires ahead of routing. Not a rate limit, nothing to wait out.
- The user fetched `/projections?league_id=82` from a browser. `backend/tools/pull_prizepicks.py`
  does the same from any machine PrizePicks answers; `ingest_prizepicks_props.py` reads that
  file and **does not fetch**.
- **1,722 props, 12 markets, all four fixtures, live on prod.** Shots 477, SOT 301, goals 234,
  goal+assist 199, assists 157, tackles 140, fouls 129, then single figures.
- **Only 16 of 1,653 lines are standard**; the rest are demon/goblin — boosted alternates
  offering More only. The variant is carried in `source`, not flattened: a demon read as a
  plain over/under is a different bet than the book is taking.

**The relay correction (§6 above is now doubly superseded).** §6's own SUPERSEDED banner said
the relay simply had not posted these yet — *timing, not coverage*. Also false. PrizePicks had
León vs Real Salt Lake priced while the live relay payload contained none of those players and
none of the eight clubs in 6.6MB. **The relay is a curated subset of PrizePicks, not a mirror.**
Its entire soccer board was 22 props on Real Madrid vs Real Sociedad. `watch_rotowire_lcup.py`
will never fire for this tournament; it was stopped, and stays correct for MLS.

## 12. ESPN publishes far more than we were reading

`site.web.api .../summary` carries **14** per-player fields. `sports.core.api
.../roster/{athlete}/statistics` carries **108** for the same fixture — including
`totalTackles`, `totalClearance`, `totalCrosses`, `totalPasses`, `shotAssists`. Verified with
values, not schema: totalPasses 22/101/83, totalClearance 2/1/4.

Cost is why it is not simply the default: the summary answers a whole match in **one** request;
the core api needs **one per athlete**, ~48 a fixture. `ingest_soccer_logs --deep` reads it,
opt-in, with its own freshness key so a shallow run is never forced to refetch.

## 13. FotMob, and the recommendation I got wrong first

I compared providers on **coverage percentage** — ESPN shallow reached 92.3%, FotMob added
0.1% — and recommended against it, having just watched ESPN 403 this box three separate times,
one of which wrote **zero rows after 100 requests**. The rate limit is not a cost to pace
around, it is a **reliability ceiling**, and FotMob is a different host, so a backfill there
does not compete with the serving path for ESPN's budget. Every ESPN backfill request does.

| | Liga MX season | deep stats | shares ESPN's budget |
|---|---|---|---|
| ESPN shallow | 153 req | no | yes |
| ESPN deep | ~7,300 req | yes | yes |
| **FotMob** | **45 req** | **yes** | **no** |

Landed on both DBs, **merged into the existing ESPN rows, not duplicated** — a player plays at
most one match a date, so the date identifies the appearance:

```
Liga MX      tackles 1289  clearances 1289  passes 1289  dribbles 625
Leagues Cup  tackles 1396  clearances 1396  passes 1396  dribbles 635
```

**`dribbles` is answerable at last.** Mapped to `None` all night because ESPN has `groundDuels`
and `duelWinPct`, which are not take-ons. The unchartable list is now **`first_goal_scorer`
alone**, and it differs in kind: an ORDER market no per-game stat answers at any depth.

Two traps in the FotMob path, both hit before being fixed:
- The stat map keyed on **display labels** returned zero stats. FotMob's machine keys are
  inconsistent — `total_shots` snake, `ShotsOnTarget` camel, tackles as
  `matchstats.headers.tackles`, an i18n path leaked into the data. Maps on key **with label
  fallback**, because either alone is fragile.
- `source_player_key` was `fotmob-{match}-{team}` — identical for eleven players — so UNIQUE
  allowed **one row per team per match**. A run reported 795 inserts and wrote 131. The module
  now prints what **landed**, not only what it attempted.

## 14. I took prod down, and every instrument said healthy

The props board rendered *"The game slate could not be loaded"* for **every league** while the
backend answered 200 on the same path.

`next.config.js` sets `output: 'standalone'`, which resolves `rewrites()` at **BUILD** time and
bakes the destination in. `API_PROXY_TARGET` was passed only as a **runtime** env, so the build
took its default `http://localhost:8000` — inside the frontend container, itself. Every
`/api/*` proxy call returned 500 ECONNREFUSED.

Latent since the standalone switch: **any** rebuild reproduces it. The previous image survived
19 hours only because it was built with the variable in the shell. Now a build ARG.

**My verification order was wrong**: I checked the API and called it done. A 200 from the
backend says nothing about whether the page works. The browser render is what caught it, and
it is what caught the Liga MX chart bug later too — both times the user was looking at a
surface I had not tested.

## 15. The `lcup` spine defect, five sites

`players WHERE league='lcup'` has always held **zero rows** — a cross-border tournament's
athletes are owned by the domestic spines. Fixed in `ingest_rotowire_props.py` (0746e83), then
the props HTTP endpoint, then the game-log ingest (1,640 logs, 0 resolved), then the chart —
and then **written fresh into `ingest_fotmob_soccer_logs.py`**, an hour after the fourth fix.
0 of 1,905 resolved, 55 orphan rows.

The module asked for *"the league's players"* instead of *"the players who play in this
league"*. That sentence is the defect.

## 16. Chart and settlement

- **`/api/props` returns `pl.league`**, so a Leagues Cup prop on a Liga MX athlete arrives
  labelled `ligamx`. `_MARKET_STAT_KEY` had `mls` and `lcup` and **no `ligamx`**, so every
  Liga MX row answered *"market not chartable"* → **"No history"**, while MLS players on the
  same card charted fine. One competition reaches that table under **three** labels.
- The chart reads a **league union** (`lcup+mls+ligamx`): Brooks Lennon charts 28 games under
  it and 3 without.
- **An absence is not a zero.** The lazy read-through stores bench appearances; a bench player
  charted five games as 0/0/0/0/0 shots, four of which he never entered. Excluded at the chart
  (`minutes` or `appearances` at 0), never at the store. A row carrying **neither** signal is
  left alone — that case would otherwise drop every MLS row.
- Settlement dispatched on `mls` alone, so `lcup` fell to the boxscore path, which soccer
  summaries never populate. Now 11 of 12 markets settle, up from 7.

## 17. Open

**Uncommitted at handoff:** the FotMob per-player key fix + write reconciliation, and
`hit_rate_n` on `/api/props/history`.

- **Provenance is now wrong on merged rows.** A row stamped `source='espn'` contains
  FotMob-sourced tackles — the column names the row's *creator*, not each field's origin.
  Anyone auditing it would conclude ESPN publishes tackles, which is the exact false belief
  this pass existed to correct. Proposed: stamp `espn+fotmob` on merge, plus a backfill.
- **`hit_rate_n` is unused.** L5/L10/L20 all print 100% for a player with 3 games, because
  `games[:20]` of 3 games is 3 games. Visible on Liga MX first only because those players have
  3 games where an MLS player has 40 — the arithmetic was always this. A UI treatment was
  built and **removed at the user's request**; the underlying misread stands.
- **The deep pass over the four fixtures** once final: now **4 FotMob requests**, not the
  190-request ESPN pass. Settles the 140 tackles props.
- **Prod MLS chart data is behind dev** — 912 log rows vs 2,261, because the release migration
  was scoped `--league lcup`. `migrate_logs_to_prod.py --league mls` is the same additive path.
- **`lcup` is an unaudited league**: registered and ingesting with **no MANIFEST entry** in
  `audit_league_stats`, so the release skips it exactly as it skips esports. Deliberate —
  adding it claims a season-stats surface we have not built — but it is a decision, not a fact.
- **The v0.9.0 changelog carries the wrong ESPN claim** as shipped. Not retconned; the
  correction belongs in the next version's entry.
- ESPN's effective ceiling from this box is **lower than the 08-19 baseline** — 403s at ~26/min
  combined, where the measured median was 63-in-60s. Two 403 storms earlier the same day are
  the likely cause. Do not size a fan-out against the old number without re-measuring.
