# 2026-08-24: Legendary Picks

Written 12:50 CDT. Supersedes nothing; the 08-19 summary is still the open-items
index for anything not touched today.

## THE DAY'S ONE SHAPE

**Four instruments each reported healthy while the thing they measure was broken,
and in three of the four the instrument's own design made the failure invisible.**

| instrument | what it said | what was true |
|---|---|---|
| `systemctl` on a monotonic timer | `enabled`, `active`, NEXT=n/a | nothing scheduled since 08-21. Bovada props 3 days stale |
| a batch ingest's exit code | `0`, success | it had just 403'd the live site's ESPN budget |
| `data/http-spend.jsonl` | 8 refusals from a gated endpoint | none of the 8 requests existed. Mocked |
| `.claude/skills/espn-request-budget` | "the limit is a COUNT, pacing does not save you" | superseded on 08-19 by a measurement in this repo |

The fourth one is the root cause of the second. A document that states a
superseded measurement as current fact is not stale documentation, it is an
instruction, and it was followed.

## 1. v0.8.7 shipped (09:38)

`a7b1251`. RotoWire relay scheduled. Kambi dropped as unused (`556ad6f`).

## 2. Bovada props were never parked

`legendarypicks-props{,-prod}.timer` were `OnBootSec=3min` + `OnUnitActiveSec=30min`
and had been sitting in `SubState=elapsed` with **no next elapse** since
2026-08-21 11:08. `is-enabled` said enabled, `ActiveState` said active,
`list-timers` said `n/a`. A monotonic timer whose reference activation systemd has
forgotten has nothing left to schedule and never says so. The journal only reaches
back to 08-23 so it could not answer either.

Two prior sessions wrote this up as "parked since Aug 21 with no recorded reason"
and put it in the task spec as a **decision**. There was no decision.

Both are now `OnCalendar`, staggered 15 minutes, sharing one flock on
`/run/lock/legendarypicks-bovada.lock`, `TimeoutStartSec` 300 to 600. Verified
armed and both ran green: atp 141/188, mlb 828-831/846, wta 108/120. dev 67,055
bovada rows, prod 60,216, `quick_check ok` on both.

**Rule now in the task spec: every timer the props runner installs must be
OnCalendar.**

## 3. A Bovada team market is not a player prop (`fb094df`)

The un-park immediately exited 3 on nba: 0 of 120 resolved. Not identity, `players`
holds 871 NBA rows. Bovada files team totals under a display group called
"Score Props", and `Highest Scoring Quarter Total Points O/U - Boston Celtics`
splits on `" - "` exactly like `Total Strikeouts - Ryan Gusto (MIA)`, so the club
landed in `player_name`. **Bovada publishes no NBA player props at all right now.**

Fixed at the parser, where the fixture's competitors are known, not at the resolver,
which can only report that nothing matched. The exit-3 guard is untouched: it is what
caught this. New report line: `team markets parsed as player props and dropped: 264`.

## 4. We store RotoWire props and grade none of them

```
source                rows    graded   pct
bovada              60,216    47,415  78.7
rotowire:prizepicks  1,100         0   0.0
rotowire:underdog      530         0   0.0
rotowire:sleeper       404         0   0.0
```

Three causes, measured:

1. **894 of 2,034** sit on markets `resolve_market()` cannot resolve at all. Every
   MLS market is in this bucket, all 478 rows: `passes_attempted`, `saves`, `shots`,
   `chances_created`, `clearances`, `crosses`, `shots_on_target`. Plus NFL
   `interceptions_thrown`, `kicking_points`, `field_goals_made`, `total_touchdowns`,
   `passing_touchdowns` and five more. These are the depth markets RotoWire was
   brought in for.
2. **10 of 25** RotoWire MLS fixtures carry an **empty-string** `espn_event_id`, so
   `settle_game()` has nothing to pull a boxscore with. Note the shape: an `IS NULL`
   count reports 0 missing on those rows, and the first count taken was exactly that
   and was wrong.
3. The remaining **1,140** are mappable and simply never settled.

Collection side: the daily archive keeps the whole relay, 3,191 props today, of which
we ingest about 9%. Never read: MLB Game 1050, WNBA 298, CFB 286, CS2 205, NFL Season
743, NHL Season 81. Cheap (3.7 MB total, ~600 KB/day) and deliberate, but there is no
retention policy. All of it is ROADMAP §10 with the numbers inline (`756e6bc`).

## 5. UFC

- `legendarypicks-ufc-fight-stats-prod.timer` was a **second silently-off prod unit**,
  valid `OnCalendar`, just never enabled. Enabled and verified after a by-hand run.
- **`10a8ff2`, the fix that matters.** Two fighters skipped every run:
  `Sergey Spivak` / ESPN `Serghei Spivac`, `Stanley Dorsainvil` / ESPN `Stan Dorsainvil`.
  All four resolver ladders compare the target's own name, which is exactly what a
  two-publisher vocabulary defeats. But a fight has two sides, and `_fighters_from_card`
  was reading the opponent's name while **discarding the opponent's ESPN id**. New
  ladder, second after exact: if we already own the publisher's id for the other side,
  the fight identifies this side without reading this side's name at all. Deliberately
  not gated on any name agreement. Ambiguous name keys map to `None` rather than
  picking one.
- `09f3bcf`: the plan's abort now NAMES its source errors. "19 source errors" with no
  names is unactionable and cost an hour of wrong diagnosis.
- Prod `players` UFC unresolved went 5 to 2 across the day. Two ids were written by
  hand, both confirmed by opponent id on both sides of the fight, never by name match.
  Remaining: Wellington Turman, Aleksandr Rakic, both with no fight in the 14-day
  current-card window to anchor on.
- **Dev's UFC spine is 109 of 117 unresolved** and prod's is 2 of 97, because the unit
  only ever existed for prod. Dev also spells him `Serghei Spivac`, ESPN's spelling,
  where prod has Bovada's. Fifth instance of "the fix landed on one environment".

## 6. Codex: the props provider registry

`/root/TASK-props-provider-runner.md` was rewritten to ~817 lines: a decision-free v2
on top, v1 preserved underneath as HISTORY with the three sections that now contradict
it struck through. §0 is an ownership table. **Codex owns three files in `backend/`.
Claude owns everything under `/etc/systemd/system`, every `systemctl`, every prod run.**
No unit file appears in the deliverable at all.

Codex caught a real contradiction before writing code: §0 forbade `systemctl`, §E check
10 required `systemctl list-timers`. Amended rather than let it substitute a git proxy,
because reading INSTALLED host config is exactly what two release gates failed to do on
08-19. `list-timers` is permitted; every mutating verb is not.

It landed `53d4440` (22 minutes): `run_props_ingest.py` (+380), `test_props_runner.py`
(+222), `monitor_props_freshness.py` reworked to a per-source query (+98/-25). Its own
run of the new monitor reports, and these are real:

```
ALERT [dev]  UNKNOWN SOURCE test
ALERT [dev]  underdog STALE: last capture 2026-08-23T02:07 (38.8h, threshold 2h)
ALERT [prod] UNKNOWN SOURCE test
ALERT [prod] underdog has no props in DB
```

**Production and unit installation are mine, not codex's.** The 8-unit to 5-unit swap
is still to do. Baseline to preserve: `systemctl list-timers --all | grep -c legendarypicks`
= **24**.

## 7. The ESPN incident, 16:48 to 17:06 UTC. I caused it.

Measured from `data/http-spend.jsonl`, minute by minute. Baseline that day was
`ingest_scoreboards` at a steady 4/min.

```
16:48  n=56   run_ufc_current_card_ingest=52   all 200
16:55  n=67   run_ufc_current_card_ingest=52   all 200
17:00  n=65   settle_props=50, link_prop_games=11
17:01  n=50   403:6      <- refusals begin
17:02  n=11   403:11
17:04  n=30   403:30     <- 26 of these are UVICORN, the serving path
17:05  n=12   403:8
17:06  n=4    all 200    <- cleared. four minutes, start to finish.
```

Three things this settles:

1. **The refusal follows the minute.** Two jobs, each well inside any plausible per-run
   ceiling, combined into a rate that was not.
2. **The budget is shared with the request handlers.** A batch fan-out made the live
   site read as broken.
3. **The block clears in minutes.** A session that day wrote "that budget is spent for a
   while" and stopped work on it. Four minutes.

Root cause was the skill file (see the table at the top). Fixed:

- `.claude/skills/espn-request-budget` §1 now leads with the burst rate; the count model
  is kept as §1b with what was right about it (`909bef4`).
- `espn_client.batch_pacing()`, one home for the four knobs a batch caller must set:
  1.5s spacing (about 40/min against a measured 36-to-63 onset band), per-host budget
  with cooldown, retry ladder, `on_exhausted="sleep"`. Restores all four on the way out
  including on an exception, because the fetcher is process-global (`7dd5a32`).
- Applied at the function that FANS OUT, never at `main()`: `build_plan`,
  `build_current_card_plan`, `settle_props.main`, `pregenerate_game_stories.discover`
  (`dc944ac`, `0375a4e`). `ingest_scoreboards` already paced itself.
- `docs/DESIGN-request-budget.md` §1c records the whole thing (`f583fe4`).

Verified: prod UFC service after the change spent 4 requests over 4 seconds, all 200.

## 8. The spend log was lying, and I repeated the lie

I reported that the test suite asks the gated `sports.core` NBA endpoint 8 times a run,
6 of them 403, and wrote it into the design doc as an open item. **Zero of those requests
exist.** `test_ingest_nba_stats.py::FetchPacingTest` drives a MOCKED `urlopen` through a
fixed 403/429/404 sequence against a real NBA core URL, and `record_spend` logged every
attempt. Proven by running that one file with the log redirected: 8 rows, 6/1/1, no
network.

Third appearance of this defect, after the 102 phantom 403s to a league named `test` that
the 08-19 analysis had to exclude by hand. Fixed at the instrument this time:
`record_spend` refuses to write when a test run targets the production log, keyed on the
DESTINATION so a test that redirects the log still gets its rows.

To attribute a live call to the test that makes it: patch `urllib.request.urlopen` in a
pytest plugin and record the nodeid. Measured that way the suite makes **13 live ESPN
requests under the prod DB, 16 under dev**, none gated:

```
9  test_leagues_hub_assertions.py::test_leagues_hub_contract
3  test_story_form_season.py::TestAgainstTheRealDatabases   (prod DB)
3  test_team_aggregates_contract.py, 3 test_ufc_rankings_pipeline.py   (dev DB)
1  test_scoreboard_range_backfill.py::TestFetchRangeChunk
```

## 9. Open

- **The 8-to-5 unit swap** for the props runner. Mine. Timer count must stay 24.
- **Dev has no UFC unit** and a 93% unresolved UFC spine. The dev fix is to run that
  ingest against dev, which now that it is paced should be attempted again; the earlier
  attempts failed with 49 identical `scoreboard_unavailable:HTTPError` because they were
  the thing spending the budget.
- **The UFC plan aborts wholesale on any source error.** A narrower in-process build
  produced 26 targets, 25 logs, 24 identity updates and threw all of it away. That is
  mechanically why dev has never accumulated anything.
- **The opponent-id ladder bootstraps.** It gives nothing on a spine where nobody is
  resolved yet, which is dev.
- **underdog is stale on dev (38.8h) and absent on prod**, per codex's new per-source
  monitor. Not investigated.
- **`UNKNOWN SOURCE test`** alerts on both envs: a `test` source has rows in `props`.
- **RotoWire depth markets** (ROADMAP §10) and the archive retention policy.
- `backend/picks.db` and `backend/picks.dev.db`, two 0-byte strays from 08-23 sitting
  untracked beside the real DBs in `data/`. Left alone rather than deleting files I did
  not create.
- **14 commits unpushed on dev.**

## 10. Commits

```
f583fe4  Record the 08-24 confirmation: the burst budget is shared with uvicorn
0375a4e  Pace the two other fan-outs that were measured bursting
7dd5a32  One home for the four knobs a batch ESPN caller has to set
909bef4  The ESPN skill taught the superseded count model, and that cost a 403
dc944ac  Pace the UFC fan-out: a batch job must not spend the serving path's budget
09f3bcf  Name the source errors instead of only counting them
10a8ff2  A fight has two sides: resolve a fighter on the opponent's ESPN id
53d4440  Run props providers through one registry            (codex)
756e6bc  Roadmap: audit the RotoWire props we store and never grade
ee56275  MLS 2026 game logs exist; the staleness expectation was asserting history
fb094df  A Bovada team market is not a player prop
0c3ea0f  MLS: a board of another competition's rows is not a failure
c9c035c  roadmap: RotoWire relay scheduled (v0.8.7); queue props-expire-at-kickoff
a7b1251  chore(release): v0.8.7
29c7dc5  changelog: v0.8.7
556ad6f  Drop Kambi: unused
```

Plus `71e77e1`, the spend-log instrument fix from §8. Suite 1,722 green on both DBs
after it. **14 commits unpushed on dev.**
