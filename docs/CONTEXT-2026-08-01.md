# Context Summary — August 1, 2026

## Current boundary

- Repository: `/root/legendarypicks`
- Branch: `dev`, 16 commits ahead of `origin/dev`
- Current code candidate: `ef4a7b3` (the documentation commit follows it)
- Nothing from this session has been pushed or deployed to production.
- Managed DEV remains frontend `:3096`, backend `:8096`, through
  `https://shortly-calendar-vast-kansas.trycloudflare.com`.
- Preserve unrelated worktree state. In particular,
  `backend/data/esports_team_logos.json` is a pre-existing tracked edit and all
  unrelated untracked files belong to other work.

## Correction that is still open

The NFL draft-board goal is **not complete on managed DEV**. The UI and API code
now implement the required projection contract, but managed DEV's
`nfl_player_projections` table has zero rows. The public UI therefore renders the
correct `PROJ 2026 PPR` column with an honest em dash for every player.

Do not describe this as complete or as real projections being live until the
projection snapshot has been published to managed DEV and the public browser
gate has been rerun. A DEV database write still requires explicit user
authorization. No production write is authorized.

## Exact draft-board acceptance contract

Recovered from `.hermes/plans/v0613-recut-executable-2026-07-31.md`, phases 4–6:

`RK | PLAYER | BYE | ADP | PROJ 2026 PPR | AVAILABLE`

- `RK` is ESPN's published 2026 PPR draft rank, not a local row index.
- `PROJ` is the season-long LP PPR total computed from ESPN's published 2026
  projected stat line with the tested position-aware formula.
- Missing source projections remain null and render `—`, never `0.0`.
- Default order is published rank; projection, ADP, availability, bye, prior-
  season points, and prior-season xFP remain explicit sort choices.
- Prior-season PPR/xFP are not the main decision columns. They remain available
  as research/sort evidence and in player detail.

## Commits added today

| Commit | What |
|---|---|
| `09fc934` | Correct the separate `/leagues/nfl` Player Rankings pool: injury tags, only QB/RB/WR/TE/PK/DEF, no TQB |
| `829fc12` | Record the missed third-pool correction in context and roadmap |
| `6ee27fc` | Restore published rank, 2026 PPR projection, bye, ADP, and availability columns across all NFL draft-board/pool renderers |
| `ef4a7b3` | Harden the browser position-label gate after position moved into the player cell |

Earlier local commits from the same continuous session remain summarized in
`docs/CONTEXT-2026-07-31.md`. They include the ESPN general-news/RotoWire
fantasy-news separation, link-free fantasy cards, injury tags, orange Overview
card, and removal of `RB2 by ADP — not our ranking`.

## Code now implemented

### Both APIs

- `/api/nfl/mock-draft/pool` returns `espn_ppr_rank`,
  `proj_ppr_points`, `proj_season`, and `proj_source` with honest nulls.
- `/api/nfl/draft-board` joins the same optional projection table, supports
  `sort=rank` and `sort=proj`, defaults to rank, returns the 2026 bye week, and
  keeps unsupported positions out.
- The detail endpoint already returns `proj_2026_pts` and the normalized
  `projection_2026` stat line when a row exists.

### All three player-pool renderers

- Pre-draft `/mock-draft` pool: `RK | PLAYER | BYE | ADP | PROJ 2026 PPR | AVAILABLE`.
- In-draft Players tab: the same columns plus the one row-action column.
- `/leagues/nfl` Player Rankings: the same decision columns, followed by the
  user's Rank/Watch/Fade controls.
- Position moved into the player cell beside team; injury tags remain beside
  the player name.
- Player overlay now labels the draft-season total separately as
  `PROJ 2026 PPR points` above completed-season scoring.

## Projection source and measured data

Official ESPN 2026 `kona_player_info` response fetched read-only on August 1:

- 11,516 total entities
- 1,359 entities with a 2026 projection stat map across all ESPN positions
- 522 draftable QB/RB/WR/TE/PK/DEF source projection maps
- SHA-256: `f52108b0ca68ae12974cbaf42d3d131e53397a4ac46e53248222ddc481769cef`

The fail-closed publisher was run only against a disposable copy of the DEV DB:

- 11,516 matched identities and 11,516 stored season rows
- 483 usable LP PPR totals
- zero `0.0` projections fabricated from unsupported/empty stat maps
- one payload checksum
- coverage: QB 65, RB 91, WR 166, TE 97, PK 32, DEF 32
- 32/32 D/ST projections

The difference between 522 draftable source maps and 483 LP totals is honest:
return-only or otherwise unscored maps remain null under the explicit formula.

## Verification evidence

### Automated

- 105 focused backend tests passed:
  `test_nfl_offseason_api.py`, `test_nfl_mock_draft.py`,
  `test_nfl_ppr_scoring.py`, and `test_nfl_dst.py`.
- 13 focused frontend tests passed, including new projection rendering and
  rank/projection sorting regressions.
- Python compile and `git diff --check` passed.
- Changed-file TypeScript diagnostics were empty; the repository-wide check
  still reports unrelated pre-existing Flow/missing-module errors.

### Disposable clone browser

- `REG-projection` passed with the exact rendered header and 483 projections.
- `REG-sort` passed for Rank, Proj Pts, ADP, Availability, and Bye.
- `REG-labels` passed for K and all 32 D/ST rows.
- Pre-draft pool rendered:
  - Jahmyr Gibbs: RK 1, bye 6, ADP 1.7, PROJ 364.7, Q, 17/17
  - Bijan Robinson: RK 2, bye 11, ADP 2.7, PROJ 351.6
  - Puka Nacua: RK 3, bye 11, ADP 3.7, PROJ 356.2
- `/leagues/nfl` rendered the same Gibbs values and injury tag with zero
  console/page errors.

Temporary evidence paths, which may disappear after a reboot:

- source payload: `/tmp/espn_2026_kona_current.json`
- populated clone: `/tmp/lp-proj-audit-PkIHEN/picks.dev.clone.db`
- screenshots: `/tmp/lp-proj-pre-draft.png` and
  `/tmp/lp-proj-league-rankings.png`

### Managed DEV/public tunnel

- Public `/leagues/nfl` renders the new headers, Gibbs RK 1, bye 6, ADP 1.7,
  Q tag, and 17/17 availability with zero browser errors.
- Its `PROJ 2026 PPR` cell is `—` because managed DEV still has zero projection
  rows.
- Both public APIs return the new projection fields, but measured projected
  counts are zero.

## Next authorized action

If the user explicitly authorizes the managed DEV database write:

1. Confirm backend `:8096` still targets the absolute path
   `/root/legendarypicks/backend/data/picks.dev.db`.
2. Create and verify a timestamped backup before mutation.
3. Re-fetch or verify the ESPN snapshot and checksum if the `/tmp` artifact is
   gone; do not silently substitute a different payload.
4. Run `backend/ingest_nfl_projections.py` with absolute `LP_DB_PATH` and
   explicit `ESPN_SNAPSHOT` against managed DEV only.
5. Verify 11,516 season rows, 483 non-null totals, zero fabricated zeroes,
   32/32 D/ST, one checksum, and `PRAGMA quick_check=ok`.
6. Verify both public APIs and all three public browser surfaces show numeric
   2026 projections, correct sorting, injury tags, and zero TQB rows/errors.
7. Record the publication evidence in the append-only roadmap/context and make
   a scoped documentation commit.

Do not push, deploy production, restart managed services, or write production
data without separate authorization.
