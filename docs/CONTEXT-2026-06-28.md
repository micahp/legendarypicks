# CONTEXT — 2026-06-28 — read FIRST after a context reset

Continues `CONTEXT-2026-06-27.md`. Also read `AGENTS.md §0` (it points here + to the runbook/specs).

## Where things stand
- **Prod = v0.2.2**, live at **legendarypicks.xyz** (docker compose stack on this host: `:3100` frontend
  / `:8100` backend, prod DB `backend/data/picks.db`). See `docs/RUNBOOK-prod-promotion.md` before any deploy.
- **Branch `analytics-backbone` = `dev` = `f4c3b64`** (pushed). All session work is here.
- **Hermes is idle.** Its UFC worktree was merged + torn down.

## Running services (detached `setsid` daemons — NOT Claude Code shells, survive a reset)
- **Dev backend** `:8095` — `LP_DB_PATH=backend/data/picks.dev.db`, log `logs/dev-backend.log`.
- **Dev frontend** `:3095` — log `logs/dev-frontend.log`.
- **Dev tunnel** → **https://hill-sharing-public-optimization.trycloudflare.com** — log `logs/dev-tunnel.log`
  (quick-tunnel; URL changes if restarted). The Stats page UFC view is live here: `/stats` → UFC.
- Restart any of them with the same `setsid env … uvicorn …` / `setsid bash -c 'exec node_modules/.bin/next dev -p 3095'`
  / `setsid cloudflared tunnel --url http://localhost:3095` pattern (see this session's history), logging to `logs/`.
- **Worktrees** (`scripts/hermes-worktree.sh`) **no longer auto-spawn a tunnel** — agents verify on localhost.

## Shipped this session (all merged to dev, mostly NOT deployed to prod)
- **UFC rankings tab** (Hermes, merged `f4c3b64`): `backend/ingest_ufc_rankings.py` (scrape ufc.com +
  `backend/data/ufc_rankings_seed.json` fallback) → `ufc_rankings` table → `GET /api/ufc/rankings`
  (P4P men/women + 11 divisions) → UFC view on `pages/stats.tsx`. **Verified rendering** (Volkanovski/Gaethje
  show). **In dev only — NOT in prod.**
- **Props market filter** scoped per-league (was one static list bleeding MLB markets onto NBA, etc.). Tunnel-verified.
- **AI game previews generate on scoreboard load** (`_core.kick_game_stories` on `/api/{league}/games`,
  background threads) instead of lazily on click; `backend/pregenerate_game_stories.py` is a manual backfill. No cron.
- **Esports opportunity writeup** `docs/WRITEUP-esports-opportunity.md` — researched + answered (market ~$18.5B,
  Dota=cleanest data, CS2=best bettors/worst data, Riot=approval-gated; MLS is Apple-TV-exclusive, corrected).
- **Homepage** "Prop Data" card fixed (was "coming soon") — **deployed to prod**.
- `scripts/hermes-worktree.sh`: auto-tunnel stripped.

## Open / next
- **Deploy decision: UFC + the rest of dev → prod?** Would be **v0.2.3**. Follow `RUNBOOK-prod-promotion.md`
  (migrate any new data into `picks.db` first; UFC needs the `ufc_rankings` table built in prod via
  `ingest_ufc_rankings.py` against `picks.db`).
- **v0.3.0 is gated on the UI holes in `docs/SPEC-v0.3.0-ui-holes.md`:** richer Stats (player/team,
  off/def), non-MLB game detail + the 3 empty tabs, post-game recap, prop outcomes on game detail,
  **UFC build-out** (props/stats now have rankings; still want per-fight strike/takedown data for modeling),
  **WC/CoD bracket + pick'em**, **context-aware filters** (props one done — sweep the other pages).

## Working rules (in memory; don't relearn the hard way)
- **"Delegate" = Hermes** (DeepSeek, no Claude quota) via the **tmux `hermes:0.0` pane** + `hermes-worktree.sh`,
  NOT a Claude subagent. Every Hermes feature task spec must require **executor + validator subagents for
  BOTH backend and frontend**, and the frontend validator must **visually test the UI in a headless browser**
  (200 ≠ acceptance). Verify every Hermes "done" myself before merging.
- No Claude attribution on commits. Dev DB only for dev work. Light tests deferred until after the entity-UX phases.
