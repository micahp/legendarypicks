# RESUME — 2026-06-24 — read this FIRST after a context reset

Orchestrator (Claude) handoff. The swarm (reasonix + hermes) runs in tmux and **keeps running
across a context reset** — they are separate processes. After reset, re-attach by checking the
panes (see §4). Verify every agent "done" yourself against real data (CEO caveat: agents claim
done prematurely).

## 1. Branch + commits
- Branch: **`analytics-backbone`** (created from `dev`; `dev` is 14 ahead of origin/dev, unchanged).
- Recent commits on this branch:
  - `8a3987f` feat(M3): World Cup soccer support — new sport class
  - `a650cab` docs(M1): offseason-data recon + storage spine design
  - `a9badb8` feat: mlbam_id backfill + analytics-backbone + world-cup spec + swarm context
  - `dee1133` fix: COD scoreboard — pull finished matches w/ scores from breakingpoint.gg (on dev, deployed)
- **Nothing pushed.** CEO pushes/deploys.

## 2. Master checklist status (see `docs/CONTEXT-2026-06-24.md` for full table)
- **Layer 0 (settlement health) — DONE + verified.** NOT broken (earlier "stale 8 days" was a
  misdiagnosis — it was identity coverage). `scripts/backfill_mlbam_id.py` resolved 204 MLB
  players → graded props 4,804 → 10,393 (76%). Settlement runs daily (latest today 06:00).
- **M1 offseason recon + design — DONE + verified** (`docs/OFFSEASON-DATA-DESIGN.md`, reasonix).
  Curl-verified: NFL→nflverse, NHL→nhle.com seasonTotals, NBA→hoopR(2023)+ESPN. Storage spine
  proposed. I independently re-verified the NBA boxscore claim (Hartenstein, event 401769991).
- **M2 storage redesign — DONE** (folded into OFFSEASON-DATA-DESIGN.md §3 by reasonix).
- **M3 World Cup soccer — DONE + verified** (commits above, hermes). I verified all 3 acceptance
  criteria against live ESPN data: `/api/wc/games` (draws + FT), `/api/wc/standings` (12 groups,
  W/D/L/GF/GA/GD/Pts), boxscore (possession/shots/cards). NOT deployed. Nit: scores render as
  floats (`5.0`) — cosmetic, same as COD client.
- **M4 persist-voids — IN PROGRESS (hermes, tmux `hermes:0.0`).** Just delegated. hermes to make
  settlement.py persist voids (hit=NULL) so cron stops re-voiding ~7400 residue props every 30 min.
  **Verify:** run `cd backend && venv/bin/python settle_props.py` TWICE — 2nd run must show ~0
  reprocessed. Check `logs/AGENT-M4-hermes.md`.
- **M6 odds-capture DESIGN — IN PROGRESS (reasonix, tmux `deepseek:0.0`).** Read-only doc task.
  Output will be `docs/ODDS-CAPTURE-DESIGN.md`. Verify the doc exists + the Bovada odds-field
  curl claim is real. Check `logs/AGENT-M6-design-reasonix.md`.
- **M5 team-stats enrichment — NOT STARTED** (unblocked now M1/M2 done; assign to hermes after M4).
- **M7 Kalshi crossover / ESPN win-prob / EV-CLV-calibration compute — LATER** (depends M6).

## 3. Live DB safety
- Backup: `backend/data/picks.db.bak-20260624` (online snapshot, verified 10,393 prop_results).
- M4 will write to `prop_results` (additive void rows). Backup is in place. No DROP/DELETE allowed.

## 4. tmux + monitor
- Sessions: `deepseek` (= reasonix), `hermes`, `money` (unrelated).
- Check panes: `tmux capture-pane -t deepseek:0.0 -p | tail -15` and `... -t hermes:0.0 -p | tail -15`.
- A persistent Monitor (`/tmp/swarm_watch.sh`, task `b02c642ab`) was watching for done-claims/
  errors/drift. It may not survive reset — if gone, re-arm with `Monitor` on `bash /tmp/swarm_watch.sh`.
- reasonix = deepseek-v4-pro max-thinking (better reasoning, weaker tools). hermes = deepseek-v4-pro
  no max-thinking (better tools). Both run on DeepSeek (no Claude quota).

## 5. Resume steps
1. `cd /root/legendarypicks && git status -sb && git log --oneline -5` — confirm on `analytics-backbone`.
2. Check both agent panes (§4) — are they idle (done) or busy? If idle, read their progress logs in
   `logs/AGENT-*-*.md` and **verify their claims against real data** before marking done.
3. If M4 done+verified → commit it (selective add of `backend/settlement.py` + its log) on this branch.
4. If M6-design doc done+verified → commit `docs/ODDS-CAPTURE-DESIGN.md`.
5. Then assign next: M5 (team-stats) to hermes; M6 implementation (the actual odds capture, per the
   design doc) to hermes after M5.
6. Do NOT deploy anything without CEO sign-off. The live site (legendarypicks.xyz) is insulated from
   code edits until `docker compose up -d --build` (deploy).

## 6. Standing guardrails (non-negotiable)
- No commit/push/deploy without CEO sign-off (agents build+test+hand back).
- Verify every "done" against real data; 200 ≠ working; don't trust agent self-reports.
- No destructive DB ops without a fresh backup. No machine-wide greps/unbounded loops (locked SSH before).
- Resolve identity by ID, never name strings (AGENTS §7).
- No Claude attribution on commits.
