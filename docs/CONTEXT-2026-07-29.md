# Context Summary — Updated 2026-07-30

## Current State

- **dev branch**: latest `68e51b0` (pushed), v0.6.13 (tagged `000229e`)
- **Dev server**: running on `:3002` (ports 3000/3001 taken by older processes)
- **Cloudflare tunnel**: running on `:3002` via `terminal(background=true)` — `https://spa-man-inspector-employees.trycloudflare.com`
  - *Note*: Tunnels die inside `execute_code` scripts. Must use `terminal(background=true)` to keep alive.
- **Backend**: auto-reloading on `:8096`

## Commits (Current Session)

1. `000229e` fix(draft): validate picks against actual draft size (14-team persistence)
2. `0e1306c` docs: v0.6.13 contract, NBA publisher, league-by-league plan
3. `8719902` feat(ui): 4-stat ESPN-style rank card on player detail page
   - Added `StatRankCard.tsx` component
   - Wired into `/player/[id].tsx` (renders before projections table)
4. `2137d5c` feat: ESPN-style rank card — pool endpoint + overlay wiring
   - Backend: `_player_stat_ranks()` helper, pool returns `stat_ranks` per player
   - Types: `PoolPlayer.stat_ranks?` added
   - Overlay: DraftRoom passes pool `stat_ranks` via props to `PlayerDetailOverlay`
5. `68e51b0` fix(overlay): league rankings renders outside player-fetch gate
   - *Bug*: League Rankings was gated inside `{!loading && !error && player}`. When no draft/picks existed, fetch failed, `error` was set, and the entire content block (including pool data stats) was hidden.
   - *Fix*: Moved league rankings section before the render gate. Stats render from DraftRoom props, independent of the `/api/nfl/draft/player/{id}` fetch.

## What's Working

- **Backend**: `/api/player/{id}` and `/api/nfl/mock-draft/pool` return `stat_ranks` (league-wide position rank)
- **Player Detail Page**: Shows ESPN-style 4-stat rank card above projections table
- **Mock-Draft Overlay**: Shows league rankings even without an active draft/picks
- **Build**: `npm run build` passes clean
- **Tunnel**: Live at `https://spa-man-inspector-employees.trycloudflare.com` (serves `:3002`)

## Pending Work

1. **2026 ESPN projections + rank** (NFL v1 slice): Extend `ingest_nfl_adp.py` to capture `kona_player_info` (PPR rank, appliedTotal), add to pool endpoint
2. **Lazy-load ESPN injuries** (non-blocking, red letter format)
3. **Production readiness**: Clone prod DB, rehearse migrations, deploy. Blocked on 2026 schedule + Team Stats data gaps.

## Key Pitfalls & Commands

- **Tunnels**: Use `terminal(background=true)` for `cloudflared tunnel --url http://localhost:3002`. Scripts kill them.
- **Ports**: Dev server `:3002`, Backend `:8096`. Port `3096` occupied.
- **Tests**: `backend/venv/bin/pytest` (not global). 67/67 passed earlier.
- **npm exec**: Never run from worktree — `/root/legendarypicks/node_modules` is symlinked. `npm exec` wipes it, kills dev.
- **Build**: `npm run build` in `/root/legendarypicks`

## Git Status

Working tree clean (excluding esport test artifact). Last commit: `68e51b0` on dev.