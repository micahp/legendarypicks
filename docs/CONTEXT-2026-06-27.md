# CONTEXT — 2026-06-27 — read FIRST after a context reset

Continues `CONTEXT-2026-06-26.md`. All work on branch **`analytics-backbone`** (pushed),
**undeployed** (prod still `v0.1.0`). Tags: `v0.1.0` (prod) · `v0.2.0` (per-game-log foundation)
· `v0.2.1` (player page + prop charts + matchups + MLB dedup). Work since v0.2.1 is untagged on the branch.

## Parallel setup (IMPORTANT)
- **Me (Claude) = main tree** `/root/legendarypicks` on `analytics-backbone`, serves the dev
  backend :8095 + frontend :3095 + the Cloudflare tunnel.
- **Hermes = its own worktree** `/root/lp-hermes` (isolated). Delegate to it there; it starts its
  own dev server on :3096. No shared-tree commits. `scripts/hermes-worktree.sh` manages worktrees.
- Hermes is idle. Verify every Hermes "done" on the tunnel myself.

## Services / how to run
- **Dev backend** :8095 — `cd backend && LP_DB_PATH=data/picks.dev.db venv/bin/uvicorn sports_service:app --port 8095 --host 127.0.0.1`
  (launch via setsid+nohup; it OOM-restarts cleanly). DeepSeek key auto-read from `/root/.hermes/.env`.
- **Dev frontend** :3095 — `node_modules/.bin/next dev -p 3095` (NOT `npx` — broken here).
  `.env.local` sets `API_PROXY_TARGET=http://localhost:8095` so it never silently proxies to prod (:8000).
- **Tunnel** — `cloudflared tunnel --url http://localhost:3095` (quick tunnel; URL changes on restart).
- Box is **memory-tight** (~1GB free); next dev can OOM. Don't run two next devs unless needed.

## Shipped this session (all on analytics-backbone)
- **Player page** `/player/[id]` + `/api/player/{id}` — header, current props (charts), projections, recent games.
- **Global player search** in header (search icon on mobile → expands; inline on desktop). Title on its own line.
- **Prop charts** `<PropChart>` + `/api/props/history` — bars vs line + hit rate + projection + L5/L10/L20/home-away/vs-opp filters.
- **Matchups tab** live — `/api/player/{id}/matchups` (vs-opponent splits).
- **MLB pitcher logs** (pitcher props chart) + **MLB identity dedup** (coverage 53→90%, `dedupe_mlb.py`).
- **player_stats refreshed** to current season from logs (`derive_player_stats.py`) — NBA/NFL/NHL advanced metrics current.
- **Game page (Phase 3):** player-props section (`/api/game/{lg}/{id}/props`) + MLB games render full layout
  (detail endpoint now returns minimal context for non-NBA/NHL). MLB scoreboard cards clickable → game page.
- **AI matchup story** — `/api/game/{lg}/{id}/story`: DeepSeek **v4-pro**, `reasoning_effort: "high"`, 8k tokens,
  grounded ONLY in records/streaks/differential + player last-5 form, cached per game. On the game page.
- **Game-state fix:** never label a live/upcoming game "FINAL" — `final_score` gated on state==post; `live_score`
  + `state` added; state-aware ScoreStrip (FINAL / LIVE / SCHEDULED).

## Game-page reality (see docs/GAME-PAGES-AUDIT.md)
- **NBA/NHL:** full box score (NBA reliable when games exist; NHL flaky off-season).
- **MLB:** matchup header + AI story + player props (no box score). In season → the live surface.
- **WC:** clickable but **no real page** (dead click) — detail unsupported. Fix: remove WC from `GameCard.hasDetail`.
- Behavior by state: final→box score/final; live→LIVE + live score; upcoming→teams only.

## DeepSeek (the LLM)
- Model `deepseek-v4-pro` is a **reasoning model** — needs big `max_tokens` (8000) + `reasoning_effort:"high"`
  or `content` comes back empty (reasoning eats the budget). Cheap → reason at max. Helper: `_deepseek_chat()`.

## Design direction (from the design-skill review + persona work)
- The **unified game page** = matchup header → AI story → **prop module** (player-grouped charts, the "edge") →
  box score (secondary). NBA was box-score-only (ignored bettor); MLB was props-only (ignored fan) — unifying.
- The winning unit is **"player → props → charts"** (game page nailed it; better than the flat Props·Lines list).
- Lead with the edge (props/projections — our differentiation), demote raw stats (ESPN owns those).
- Personas: prop bettor (props.cash) · fantasy · casual fan (ESPN) · player-share trader (sport.fun, later).
- Full plan: `docs/PLAN-entity-ux-restructure.md`.

## Open / next (no priority order — keep building)
- **Remove WC dead click** (1-line in GameCard.hasDetail); gate NHL too if still flaky.
- **NBA "edge" on game page** — NBA has projections (no props); show projected lines in the edge slot.
- **PropChart mobile-responsive** (medium pri) — SVG overflows → x-scroll; make it scale to width.
- **Code org:** the game page file is getting big (ScoreStrip/GameProps/GameStory/box-score all in one) — split
  into `components/Game/*` for readability/maintainability.
- **Props page** — do NOT refactor (owner said leave it).
- Phase 4 (Leagues tab = rename Stats) · Phase 5 (remove Analytics tab) · per-page projection-vs-line badge.

## Guardrails (non-negotiable)
- **NEVER suggest stopping / breaks / "call it for now"** — keep working until told to stop ([[feedback_never_suggest_stopping]]).
- No commit/deploy attribution to Claude. Dev DB only (`LP_DB_PATH`). Resolve identity by ID, never name.
- Verify every "done" on the live tunnel (data flowing ≠ it renders right). Lead with the answer; no fluff.

## Session 3 (later Jun 27) — refactors + retro + next-phase spec
- **Frontend game page split** into `components/Game/{types,ScoreStrip,TabBar,BoxScore,PlayByPlay,GameInfo,
  GameProps,GameStory}.tsx`; `pages/game/[league]/[gameId].tsx` is now ~109 lines of composition. No
  behavior change; typecheck clean, route serves 200. (commit f73efae)
- **Backend `sports_service.py` god-file split** (was 2125 lines / 30 routes) → thin app shell that
  `include_router`s `backend/routers/{games,players,props,analytics,game_extras}.py`; shared DB/schema,
  `_helpers`, market maps, Pydantic models → `backend/_core.py` (`from _core import *`, `__all__` exports
  underscore names). Helper bodies AST-sliced verbatim, no logic change. **All 30 routes verified serving
  real data** (direct :8095 + via :3095 proxy). (commit 009063e) **Add new endpoints to the right router now.**
- **Engineering retro** (gstack `/retro`): `docs/RETRO-2026-06-27.md` + `.context/retros/2026-06-27-1.json`.
  Headline: 83 commits solo this week, **0% tests**, docs=31% of commits. **DECISION: light tests DEFERRED
  until after the entity-UX phases** (then cover projections/EV-CLV/identity first).
- **Next-phase build spec written:** `docs/SPEC-2026-06-27-next-phases.md` — the two hard pieces
  (NBA→matchups tab, Stats-page leagues w/ player+team stats) with backend+frontend acceptance criteria,
  remaining milestone UI, and UFC weight-class + P4P rankings. **Not yet implemented.**

## Session 4 (later Jun 27) — P1: NBA in Matchups tab ✅ COMPLETE
- **Root cause:** 24,086 NBA log rows had `opponent=None, home_away=None` — `ingest_nba_logs.py:95`
  passed `None, None` instead of deriving from the game-level home/away abbrevs already available
  from `espn_client.games()`.
- **Fix — ingest:** `ingest_nba_logs.py` now pulls `g["home"]["abbrev"]` / `g["away"]["abbrev"]`,
  matches player's team to determine opponent + home_away. Commit `823d85d`.
- **Fix — backfill:** New `backend/backfill_nba_opponent.py` — one-shot script grouped 24,086 rows
  by 185 dates, called ESPN once per date, derived opponent/home_away per row. 0 misses, idempotent.
- **Frontend:** Matchups tab now shows player name + team badge + **league badge** (emerald) +
  opponent count when data loads. No Bovada dependency — works off ESPN box score logs alone.
- **Verified:** Wembanyama returns 28 opponents (OKC 12gp 23.58 PTS, NY 8gp 25.5 PTS, etc.)
  through :8095 direct and :3095 proxy. Browser confirmed full table with real splits.
- **P1 done.** P2 (Stats page leagues) next; P3+P4 still pending.
