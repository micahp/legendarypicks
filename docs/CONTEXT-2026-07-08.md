# CONTEXT — 2026-07-08 session (read first on resume)

Two big threads this session: the **Metric bug-bounty audit** (Sherlock contest) and the
**LP YouTube-stream fix** (which I overcomplicated — see below). Plus momentum engine phase 1,
the Hermes prop-repair, and housekeeping.

## ⚠️ Overcomplication lesson (Micah, angry, end of session)
The ask was dead simple: "check the live streams for YouTube versions." For a known tournament
(EWC) the YouTube version = the tournament's OFFICIAL YouTube channel's current live video.
I turned it into a datacenter-bot-wall / Data-API-resolver saga before getting there.
**Do the simple thing first.** The clean fix is spelled out under "YouTube fix" below — execute
that, don't re-derive it.

## Metric bounty — Sherlock contest 1279 ($121k, ends 2026-07-27, KYC done, Micah submits)
- Local: `/root/metric-audit` (fork `sherlock-audit/2026-07-metric-micahp`).
  Pinned: core `7b9ab56` · periphery `d210a84` · poc `056c204`.
- Working notes: `/root/metric-audit/_audit/NOTES.md`. Dedup list: `_audit/DEDUP.md`.
- **OUT OF SCOPE**: oracle-shock + two-leg cycle drain (up to half bin width) — any restatement invalid.
- Known issues (don't resubmit): Zellic 5 + Collaborative H-1/M-1..9/L-1..31 — see DEDUP.md.
- **Reviewed, all sound** (6 highest-value/README-flagged surfaces): AnchoredPriceProvider band
  clip math, SwapMath rounding+conservation, reentrancy guard, factory timelock/fee-caps,
  pool.setPriceProvider (onlyFactory), swap settlement (IncorrectDelta flash-accounting).
  This is a hardened, twice-audited target — no eyeball Medium+ found.
- **Build env WORKS** (was a yak-shave): `forge 1.5.1` alpine-static at `/root/.foundry/bin`
  (box glibc too old for standard build; old curl breaks foundryup → used
  `gh release download stable --repo foundry-rs/foundry --pattern '*alpine_amd64*'`).
  OZ (`ef85270c`) + forge-std (`bf647bd`) cloned into `metric-core/lib` (submodules were empty;
  private Metric-OMM nested submodule can't clone, not needed for core build).
  `cd metric-core && PATH=/root/.foundry/bin:$PATH forge build` → succeeds.
- **Fuzzer started**: `metric-core/test/audit/SwapMathInvariants.t.sol` — invariants: (A) per-call
  accounting conservation, (B) no-free-value exact-in vs exact-out round trip. FIRST FAILURES ARE
  HARNESS BUGS (unconstrained fuzz → uint104 overflow on bin balance, a state the pool prevents),
  NOT findings. Next: bound fuzz inputs so post-swap bin balances stay ≤ uint104 (pool invariant),
  then re-run; only then are failures real.
- Realistic EV: low (hardened + twice-audited + full Watson field). Best remaining shot = the
  bounded SwapMath fuzzer. Don't manufacture findings (memory: Fable/DeepSeek confident FPs).

## YouTube fix (LP esports board) — DONE, verified on real live matches (2026-07-08)
- Landed on `feat/live-discounts`, NOT committed yet (Micah to review diff first).
  `backend/routers/esports/streams.py` + `backend/routers/esports/yt_live_resolver.py`.
- The simple fix (as planned): `_YT_TOURNAMENT_CHANNELS` in streams.py maps a tournament keyword
  → official YT channel id, title-agnostic; `_rule_candidates` always appends this candidate
  regardless of which `_WATCH_RULES` entry (if any) matched. `yt_live_resolver` resolves the
  channel's current live videoId via Data API (`YOUTUBE_API_KEY` in `/root/.hermes/.env`, valid).
- **Caught 2 real bugs while verifying against actual live matches** (2 live EWC Dota games,
  Team Spirit v Team Nemesis + Vici Gaming v PARIVISION, were on the board at test time):
  1. The channel id "verified" earlier in this session (`UC1Xqp122TsjLISeYa1EwcaQ`,
     customUrl `@esportsworldcup`) is a DIFFERENT, abandoned 56-subscriber channel that happens to
     hold that handle — not the real org. Real channel: `UCENNtCRTPTdH_IGXs42LuHQ` (handle `@ewc`,
     1.45M subs). Fixed before it ever shipped.
  2. EWC's real channel frequently multi-simulcasts (5 concurrent live videos observed: ALGS,
     Fatal Fury, Dota Stream A/B/C — titles carry no team names to disambiguate by). First pass
     grabbed an arbitrary one of the 5 (via the scrape path, not just the Data API path) and
     would have embedded the WRONG arena's game for both live Dota matches. Added an ambiguity
     gate: resolve only when exactly ONE video is live on the channel; otherwise return None and
     let the existing per-arena Twitch candidate (already correct, frag/PS-attested) win — verified
     live that both matches correctly stayed on Twitch with YouTube listed as an unresolved
     alternate. A third EWC Dota match with no other candidate now gets the YouTube channel link
     as a fallback (previously watch=None).
- Backend :8095 restarted 3x during iteration (env: `GRID_API_KEY` + `YOUTUBE_API_KEY` from
  `/root/.hermes/.env`, `LP_DB_PATH=picks.dev.db`) — running clean, no startup errors.
  `scripts/verify_yt_pick.py` stopgap watcher killed (real fix landed + verified, no longer needed).
- Not yet done: commit the diff, and no other tournament besides EWC has an entry yet (only one
  verified channel id today — add more only when actually verified, not guessed).

## Hermes prop-repair — DONE (step 1 of SPEC-prop-loop-mlb.md)
- Report: `/root/lp-prop-repair/backend/repair_props_report.txt`. Markets 1223→10, dupes→0,
  8,561 fake-zero settlements nulled, D1 parse proven (Harper), idempotent, `ingest_props.py`
  writer fixed. Ran on isolated `picks.repair.db` (live dev DB untouched). Worktree `/root/lp-prop-repair`.
- NOT applied to the real dev DB — that's a separate reviewed step. Re-settlement is a later task.

## Momentum engine — phase 1 shipped (feat/live-discounts)
- `analytics/momentum.py` (Wilder dual-MA cross_state), `ingest_team_results.py` (ESPN schedules →
  team_game_results, 2735 rows), `compute_momentum.py` (3362 states, 20k crosses), `routers/momentum.py`
  (/api/momentum/{league}/crosses|board|player|team), nightly cron `scripts/momentum_nightly.sh` 06:30 UTC.
  Display-only until the validation harness (needs prop-loop repair) passes. MLB only so far.

## Housekeeping / open
- Trading repo: `docs/philosophy-pointer` merged into local `main` (fast-forward, all 6 commits).
  **`git push origin main` is PERMISSION-GATED — needs Micah's OK or a manual push.** Not pushed.
- Widget poller cron live (`*/2` → scripts/poll_live_discounts.sh).
- LP dev backend :8095 (this tree, picks.dev.db) + :3095 frontend + crons (watch */4, monitor */5,
  poller */2, momentum 06:30) all running.
