# CONTEXT — 2026-07-07 session (read first on resume)

One marathon session: LP esports board audit + fixes → live-discounts widget classes → stakes
engine → momentum-engine spec → broadcast-alpha pipeline LIVE with first receipts → swing
backtest via Hermes. Two repos touched; everything committed + pushed.

## Repos / branches
- **legendarypicks**: work on `feat/live-discounts` (origin, HEAD ≈ `c843d90`+). `dev` rewound to
  `1eddbe0` (force-pushed at Micah's request) — widget lives on the branch, NOT dev. Dev backend
  :8095 running from this tree (launch: kill by port-pid, `LP_DB_PATH=backend/data/picks.dev.db`,
  `GRID_API_KEY` + `PANDASCORE_API_KEY` from /root/.hermes/.env, plain uvicorn, exit-144 is fine).
- **prediction-market-trading**: ⚠️ checked out on branch **`docs/philosophy-pointer`** (NOT
  master) — all of tonight's commits live there, pushed at reset-prep as `a9d1bea..eb62469`
  (`git push origin master` had been silently no-opping all night). Contents: WC watcher series,
  swing-regime note, broadcast_alpha.py + dedup, esports_price_tape.py, swing backtest. TODO next
  session: merge/rebase this branch onto master (check what the branch's original purpose was —
  it predates tonight).

## LP: what shipped (feat/live-discounts)
1. **"Cheap Quality, Live" widget** (`backend/routers/live_discounts.py` + `components/
   LiveDiscounts.tsx`, mounted in scores.tsx, league prop, default mlb,wc):
   - Class A DISCOUNT: quality dip + form gate + **rally evidence** (no-knife-catching: runners
     on, at bat) + WP-fair suppression.
   - Class B WITCHING: late+close + **edge vs ESPN live WP** (no WP → no card). Price bands removed
     (Micah: 3¢ with 10% comeback = take it — edge, not price level).
   - Class C PREPRICED: immutable pregame levels k=0.35×pregame (≥30¢ eligible, 5¢ floor),
     time-remaining guard, sovereign. Born from Micah's MEX 10¢ maker fill.
   - Class D GIFT_FADE: favorite wastes penalty (ESPN keyEvents) while not leading → fade the dog,
     20-min window. Born from ARG-EGY +2.6R counter-trade.
   - Receipts: `live_discount_log` (fire→resolve), snapshots in `live_price_snapshots`.
   - Ops gap: refresh-on-request → needs a poller cron (manual curl-loop used for WC tonight).
2. **Esports board audit vs sources** (GRID/VLR/SiegeGG/Liquipedia): fixed NRG-KC flipped winner
   (Kalshi settle fallback fabricated winners from ambiguous events — hardened: clean 2-market
   events only, disagreement → None), Lavked draw fabrication, JPlay dupe, LPH false-finish,
   Poor Rangers/Power Ranger alias dedup, PS-canonical display names, Bovada "LMap" phantom filter
   (+ `import re` self-own fixed). Backlog: pre-game GRID surfacing, draw schema, frag dead code.
3. **Stakes engine** (`backend/stakes.py` + generate_game_story): WC knockout/group scenarios +
   MLB division/GB; lead priority stakes>form>record; anti-padding; `has_stakes` regen-once.
   BRA-NOR story before/after is the demo.
4. **Specs**: `SPEC-momentum-engine.md` (Micah's multi-timeframe Wilder crossover design: fast/slow
   per (entity,stat) at player/team/live levels, expected-vs-actual axis, crash-cycle phase labels;
   VALIDATION GATE before anything feeds decisions), `SPEC-live-discounts-widget.md` (evolution
   log through v0.4+ClassD), `IDEA-broadcast-alpha.md`, `SPEC-prop-loop-mlb.md` (prop repair, NOT
   started), `LEAGUE-REGISTRY-per-game.md`.

## Trading: what shipped + learned
- **Swing regime** (`docs/NOTE-2026-07-07-live-soccer-swing-regime.md`): live soccer = multi-
  whipsaw; exit INTO the payoff, re-enter next overreaction; hold-to-resolution does NOT transfer.
  Receipts: ARG-EGY (Micah EGY 29¢→83¢ top tick +2.6R; ARG then won 3-2) and SUI 41¢→73¢ (+78%).
- **Swing backtest** (Hermes via tmux, `runs/swing_backtest.py`): swing exits beat hold on BOTH
  ledgers (live n=5: −0.03→+2.62R @ +20¢; legacy n=93: −23.8→−17.4R); tight trails HURT. Caveats:
  tiny n, no soccer in samples, same-side re-entry only. Hermes halted on a miscalibrated sanity
  gate (1e-6 vs 2dp data) — verified per-row diffs were 0.00 before overriding to 2e-2.
- **Watcher**: WC series added (KXWCGAME/KXWCADVANCE). Paper `synthesis-hold` strategy actively
  trading (5+ paper BUYs tonight, MLB/WNBA). Real money = manual only.
- **Account**: $3.65 → **$17.10** today via two manual swing trades. Prior WC-advance longshot
  streak (MEX −3.6R etc.) documented in settlements.
- **Kalshi API traps**: esports series (KXDOTA2GAME/KXVALORANTGAME) DELIST from public
  markets/events endpoints mid-day — price history unverifiable after the fact → we log our own
  (`esports_price_tape.py`, per-minute Bovada-implied for live esports). Dota event ticker order
  was `VPLGD` (not LGDVP) — candlesticks for the LGD game may be recoverable with the right ticker.

## Broadcast alpha (the night's headline)
`broadcast_alpha.py` (py3.11 venv `.venv-ba`; streamlink/ffmpeg → faster-whisper base.en int8 →
DeepSeek soft-signal extraction → jsonl receipts). **674 signals across two simultaneous live
games** on a 4-core box:
- EWC Dota LGD-VP (Bo2 1-1): every phase tracked; best class = PRE-GAME DRAFT READ (called LGD g2);
  mid-game caster theses 1-for-2 → **swing fuel, not hold theses**.
- **WC COL-SUI: FIRST PRICE-VERIFIED RECEIPT** — sustained SUI-bullish tape vs SUI-advance flat at
  38.5¢ (178 snapshots) → 0-0 → SUI on pens. ~10pt edge, documented. Micah traded it live (41¢→73¢).
- v0.2 backlog: roster/draft context into extractor (player→team attribution: "Yuma" misread),
  `watch` mode self-arms on live EWC matches from LP board.

## Open threads (priority order per Micah's direction)
1. Momentum engine build (core + MLB adapter) — spec'd, not started; validation harness needs the
   PROP-LOOP REPAIR (SPEC-prop-loop-mlb.md) which is also not started.
2. Broadcast v0.2: roster context; run watch-mode on EWC 09:00Z slate; join VPLGD candlesticks.
3. Widget: poller cron (no browser = no snapshots), esports leagues into widget (Bovada tape).
4. LP strategy context: nobody visits legendarypicks.xyz (scanner-only traffic — verified Jul 4);
   product thesis = sports brain for prediction markets; sell to the pm-scene (geoppls receipts).
   Jobs≠founder-revenue lesson in memory.
5. Sherlock contest (Jul 6-27) = open option, NOT chosen ("not my advantage").

## Live processes on this box
:8095 LP dev backend (this tree) · :3095 next dev · esports_price_tape.py (bg) · watch_live cron
*/4 · esports monitor cron */5 · broadcast listeners ENDED (both games over).


## Post-doc addendum (reset prep, ~05:00Z Jul 8)
- **Paper strategy day tally (synthesis-hold, closed trades): −1.8R** (10W/8L, losses bigger —
  hold-to-resolution shape). 10 positions still open (MLB evening slate, 2 WNBA, 1 tennis).
- **Head-to-head of the night:** synthesis-hold bought COL-advance 62¢ (−0.97R) in the same match
  where the broadcast tape + Micah bought SUI 41¢→73¢ (+78%). Pregame-numbers entry vs live-
  information swing, one game, both directions documented.
- `synthesis-hold` strategy itself was created by another session (git eb62469 META tag predates
  tonight's commits? — verify its entry logic before judging it; only one day of results).
- Both repos confirmed fully pushed. LP = feat/live-discounts; trading = docs/philosophy-pointer.
