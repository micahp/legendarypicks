# CONTEXT — 2026-07-09 (read first on resume)

Repo `/root/legendarypicks`, branch **`feat/live-discounts`** — **all work PUSHED through `a4d13fe`**.
Nothing uncommitted, tree clean (only untracked logs/scratch).

## ⭐ FIRST: read the new reference doc
**`legendarypicks/docs/ESPORTS-EXPECTED-BEHAVIOR.md`** is the source of truth for how `/esports`
should behave (state machine invariants, matcher policy, streams, logos, LMap-2, Results gaps).
Read it before editing `backend/routers/esports/*` or `pages/esports.tsx`, and update it in-commit
when behavior changes. It also has a §8 "open follow-ups" list = the backlog below.

## Remaining work (Micah's stated next priorities)
1. **Scheduled tab** — more polish/fixes wanted (not yet specified — ask/inspect on resume).
2. **Results tab**:
   - EWC (Dota especially) **result hole**: ~20 finished EWC cards show no result because no source
     (PS/GRID/Kalshi) returned one. Plus a few archived as `finished` with no winner+score that
     should be `ended_unknown`.
   - Frontend never renders a **"result unavailable" label** for `resultUnknown` — they read like
     broken Finals. `resultUnknown` is computed in backend, unused in `esports.tsx`.
   - `- LMap 2` rows: filtered on the live Bovada path, but **stale ones linger in the archive/store**
     (3-day retention) showing blank results + the `LVLUP - LMap 2` abbreviation. Strip suffix / drop
     from archive.
3. **Logo situation** (Micah: "actual Marsborne has no logo lol"):
   - Most missing logos are **genuine upstream gaps** — PandaScore has no crest for many minor teams
     (audited 22/26). NOT naming misses (canon index covers spellings).
   - `data/esports_team_logos.json` caches **negatives permanently** (marsborne/hive/syf stuck) —
     needs expiry/invalidation. NOTE: PS teams-API reported an image for "Marsborne" but Micah says
     the real org has none — reconcile (may be a placeholder). Don't assume "fixable" — verify.
   - Some crests exist **only on tournament sites** (Poor Rangers, post-rebrand, on the EWC
     participants page https://esportsworldcup.com/en/competitions/2026/dota2#participating_clubs) —
     would need an EWC/tournament logo source.
4. Low pri: `Imperial v LP` dupe ("LP" = "Largados y Pelados", too generic to alias globally).

## Done this session (part 3), all pushed — newest first
- `a4d13fe` docs: ESPORTS-EXPECTED-BEHAVIOR.md.
- `3e66404` `ex-<org>` = DISTINCT team (stop stripping `ex-`; Marsborne rebrand, Liquipedia/HLTV).
- `143b1f7` relaxed same-match merge — "a team can't play two matches at once": same-title + <=10min
  + one exactly-matched team + label-variant other side. Catches Yawara/YNG-Sharks, VfB, FaZe/
  Vasteras, OG/Inner-Circle-x-Insanity — WITHOUT loosening `_same_team` (MIBR≠MIBR LOS stays).
- `8a915a2` canon hardening: fold diacritics (Beşiktaş), generic-aware camelCase (TeamOrangeGaming/
  TheBoys — does NOT break eSports/BakS/paiN), JPlay→Just Players alias.
- `6f8f453` switcher trusts backend stream order (English-YouTube-first; no fixed platform re-sort).
- `2c572be` verify attested Twitch via decapi, drop decapi-dark streams.
- `d4955b1` game-narrow YouTube resolution (was blocked on "does YT play?" — RESOLVED: it plays for
  users; datacenter-IP bot wall only).
- `1fce1ec` drop dead-link stream when all candidates dark → phantom `running` card leaves the board.

## RESOLVED facts (don't re-litigate)
- **`ex-<org>` ≠ `<org>`** (departed roster vs org's new lineup). Keep `ex` token; it's in
  `_DISTINCT_SQUAD` too.
- **MIBR ≠ MIBR LOS, G2 ≠ G2 HEL, Vitality ≠ Vitality Rising Bees, Team Secret ≠ Whales, FaZe ≠ FaZe
  Up Next, main ≠ Academy** — deliberately SEPARATE (fail-closed `_MERGE_OK_SUFFIX` allowlist). Don't
  add their suffixes to the allowlist. The relaxed merge dedups the *cross-labeled same match* rows
  without merging the teams.
- **YouTube plays fine for end users**; bot wall is datacenter-IP-only. YouTube-default is correct.
- **English preferred**: backend ranks foreign BEFORE platform, so a foreign YouTube is NOT hoisted
  over an English stream. Frontend must not re-sort.

## Running services
- Backend :8095 — launch env: `GRID_API_KEY`+`YOUTUBE_API_KEY`+`PANDASCORE_API_KEY` from
  `/root/.hermes/.env`, `LP_DB_PATH=/root/legendarypicks/backend/data/picks.dev.db`. Restart:
  kill pid on :8095, then `setsid nohup venv/bin/python3 -m uvicorn sports_service:app --host
  0.0.0.0 --port 8095`. Cold rebuild ~9s.
- Frontend :3095 (`next dev`, HMR — never git-checkout under it).
- Tunnel: `cloudflared tunnel --url http://localhost:3095` (quick tunnel, random URL each start).

## Matcher regression guard (no committed suite — rebuild if editing the matcher)
Positives that MUST merge: 9z/9z Globant, Keyd/Keyd Stars, LVLUP/Level UP, NIP/Ninjas in Pyjamas,
Anyone's Legend/AG.AL International, WBT/Wrotberry, Beşiktaş/Besiktas, TeamOrangeGaming/Team Orange,
TheBoys/The Boys, JPlay/Just Players, BakS eSports/BAKS Esports, JUMBO TEAM/Jumbo Team.
Negatives that MUST stay separate: MIBR/MIBR LOS, G2/G2 HEL, Team Secret/Team Secret Whales,
GAM Esports/GamerLegion, GamerLegion/Legion, paiN/Ready, Pain Gaming/paiN Academy, ex-Marsborne/
Marsborne, ex-Vexa/Vexa.
