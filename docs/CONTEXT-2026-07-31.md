# Context Summary — July 31, 2026

## Active Session
- CLI session `20260731_xxxxx` — pool expansion, virtualization, fantasy news

## Commits from today's session (corrected code candidate: `fe1f296`)

| Commit | What |
|--------|------|
| `1aecda6` | Injury status display on overlay + pool, news tab on player detail (ESPN) |
| `8fb5695` | **Merge recut/v0.6.13** → 11,515 player pool, D/ST PPR ADP, batch stat ranks, injury guards |
| `2c85e5c` | **Virtualize PoolList** — 34 DOM rows regardless of pool size |
| `0eed530` | Filter pool to fantasy positions only (QB/RB/WR/TE/PK/DEF) |
| `76a8ea9` | Revert pool filter — pool stays full universe |
| `9efaa3f` | Draft room position pills filtered to fantasy positions only |
| `f4e05fb` | **RotoWire fantasy news** replaces ESPN — notes + analysis + injury badges |
| `3a5546d` | **News tab on mock draft overlay** — accessible from pool player click |
| `888fb51` | **Fantasy-news audit repair** — identity-safe matching, explicit source states, newest-first ordering, correct local dates, shared tested UI |
| `fe1f296` | **Scope correction** — ESPN general news restored on player profiles; RotoWire fantasy news isolated to the draft overlay with stable-ID/history coverage |

`origin/dev` is now at `9efaa3f`. The fantasy-news feature and correction commits
remain local/un-pushed; the context/roadmap documentation is also local.

## Running

| Service | Port | Status |
|---------|------|--------|
| Backend (uvicorn --reload) | :8096 | Live, `LP_DB_PATH=/root/legendarypicks/backend/data/picks.dev.db` |
| Next.js dev | :3096 | Live |
| Cloudflare tunnel | shortly-calendar-vast-kansas.trycloudflare.com | Live |

## What changed today

### Pool expansion (11,515 → usable)
- Backend now serves full ESPN universe via recut merge
- PoolList virtualized — only ~25-34 DOM rows at any time
- Position filter pills show only fantasy positions in draft room
- Pool stays 11,515 (IDP/coaches/punters visible but not draftable)

### Fantasy news (RotoWire)
- Available only in the mock-draft player overlay; the standalone player detail
  keeps general ESPN reporting
- Rolling XML feed (172 updates / 157 players when re-measured), validated before a 10-min cache replaces its last good snapshot
- Sleeper's published ESPN/GSIS→RotoWire crosswalk supplies stable identity;
  RotoWire player pages add public player-specific history beyond the rolling feed
- Each article: notes blurb + **FANTASY SPIN** analysis + injury badge + return date
- Same source Sleeper uses in their app

### Fantasy-news audit repair
- RotoWire update/player IDs are retained; an existing `player_external_ids`
  crosswalk wins when available
- Stable ESPN/GSIS evidence resolves team changes such as Deebo Samuel
  (`players.team=WSH`, RotoWire team `SF`, source ID `13429`) without weakening
  identity matching
- The rolling feed is not treated as complete player history; a valid player
  page can return older news even when the player is absent from those 157 IDs
- Source outage, stale cache, no-news, unsupported, and identity-unavailable are
  distinct API/UI states
- Feed is newest-first; date-only estimated returns no longer shift back one day
  in US time zones; only the fantasy surface shows `Source: RotoWire`
- Focused verification: 14 news backend tests + 13 profile API tests + 5 React
  tests; public player page and in-draft overlay rendered with zero console errors

### Injury display
- Red Q/D/O/IR badges on overlay header and pool rows
- injury_status + last_news_date in pool API response (column-guarded)

## v0.7.0 Status
- Slice A (draft notes) ✅
- R4 (schedule) ✅
- Slice D (mock draft) ✅
- T1 (D/ST ADP) ✅ — merged from recut
- T2 (pool expansion) ✅ — merged from recut
- Injury display ✅
- Mock-draft fantasy news ✅ — scope and coverage corrected in `fe1f296`
- Player-detail general ESPN news ✅ — restored in `fe1f296`
- **Prod deploy (R6) — NOT DONE**

## Key files modified
- `backend/routers/nfl_mock_draft.py` — pool expansion, injury columns, batch stat ranks
- `backend/routers/players.py` — separate ESPN general and RotoWire fantasy endpoints
- `backend/nfl_news.py` — feed/history validation, cache, and stable identity resolution
- `backend/test_nfl_news.py` — source, identity, ordering, and failure regressions
- `backend/ingest_nfl_adp.py` — full universe ingest, adp_ppr, D/ST PPR rank
- `components/MockDraft/PoolList.tsx` — virtualization
- `components/MockDraft/DraftRoom.tsx` — position pill filter
- `components/Leagues/PlayerDetailOverlay.tsx` — News tab + injury badge
- `components/Leagues/PlayerNews.tsx` — draft-only fantasy-news renderer
- `components/Leagues/PlayerNews.test.tsx` — status/date/render regressions
- `pages/player/[id].tsx` — general ESPN News tab
- `pages/mock-draft.tsx` — engine pool filter (DRAFT_POSITIONS)

## Notes
- **Never npm/npx/yarn from worktrees** — node_modules symlink to main
- Dev backend MUST use `LP_DB_PATH=/root/legendarypicks/backend/data/picks.dev.db`
- Release via `scripts/release.sh <version>` only
- RotoWire API: `rotowire-secrets-ebgmaeh8ecc4huhf.canadaeast-01.azurewebsites.net/api/proxy?feed=NFLNews`
